import React from 'react';
import EventList from '../components/events/EventList';

const Events = () => {
    return (
        <div>
            <EventList />
        </div>
    );
};

export default Events;
